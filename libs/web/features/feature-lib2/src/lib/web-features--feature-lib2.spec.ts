import { webFeaturesFeatureLib2 } from './web-features--feature-lib2';

describe('webFeaturesFeatureLib2', () => {
  it('should work', () => {
    expect(webFeaturesFeatureLib2()).toEqual('web-features--feature-lib2');
  });
});
