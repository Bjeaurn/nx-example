import { webFeaturesFeatureLib1 } from './web-features--feature-lib1';

describe('webFeaturesFeatureLib1', () => {
  it('should work', () => {
    expect(webFeaturesFeatureLib1()).toEqual('web-features--feature-lib1');
  });
});
