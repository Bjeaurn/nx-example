import { webDataAccessDataAccessLogin } from './web-data-access-data-access-login';

describe('webDataAccessDataAccessLogin', () => {
  it('should work', () => {
    expect(webDataAccessDataAccessLogin()).toEqual(
      'web-data-access-data-access-login'
    );
  });
});
