import { groupedGroupedLib2 } from './grouped-grouped-lib2';

describe('groupedGroupedLib2', () => {
  it('should work', () => {
    expect(groupedGroupedLib2()).toEqual('grouped-grouped-lib2');
  });
});
