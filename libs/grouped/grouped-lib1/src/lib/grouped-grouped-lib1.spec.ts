import { groupedGroupedLib1 } from './grouped-grouped-lib1';

describe('groupedGroupedLib1', () => {
  it('should work', () => {
    expect(groupedGroupedLib1()).toEqual('grouped-grouped-lib1');
  });
});
