import { groupedGroupedLib3 } from './grouped-grouped-lib-3';

describe('groupedGroupedLib3', () => {
  it('should work', () => {
    expect(groupedGroupedLib3()).toEqual('grouped-grouped-lib-3');
  });
});
